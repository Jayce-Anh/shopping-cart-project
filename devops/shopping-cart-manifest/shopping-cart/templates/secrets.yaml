################################ SECRETS ################################

#============== ClusterSecretStore ===============#
apiVersion: external-secrets.io/v1
kind: ClusterSecretStore
metadata:
  name: aws-secrets-manager
spec:
  provider:
    aws:
      service: SecretsManager
      region: {{ .Values.aws.region }}

---
#================== Catalog ==================#
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: catalog-secrets
  namespace: {{ .Values.namespace }}
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: aws-secrets-manager
    kind: ClusterSecretStore
  target:
    name: catalog-secrets
    creationPolicy: Owner
    template:
      metadata:
        annotations:
          argocd.argoproj.io/compare-options: IgnoreExtraneous
          argocd.argoproj.io/sync-options: Prune=false
  dataFrom:
    - extract:
        key: {{ .Values.externalSecrets.rdsSecretName | quote }}

---
#================== Inventory ==================#
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: inventory-secrets
  namespace: {{ .Values.namespace }}
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: aws-secrets-manager
    kind: ClusterSecretStore
  target:
    name: inventory-secrets
    creationPolicy: Owner
    template:
      metadata:
        annotations:
          argocd.argoproj.io/compare-options: IgnoreExtraneous
          argocd.argoproj.io/sync-options: Prune=false
  dataFrom:
    - extract:
        key: {{ .Values.externalSecrets.rdsSecretName | quote }}

---
#================== Order ==================#
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: order-secrets
  namespace: {{ .Values.namespace }}
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: aws-secrets-manager
    kind: ClusterSecretStore
  target:
    name: order-secrets
    creationPolicy: Owner
    template:
      metadata:
        annotations:
          argocd.argoproj.io/compare-options: IgnoreExtraneous
          argocd.argoproj.io/sync-options: Prune=false
  dataFrom:
    - extract:
        key: {{ .Values.externalSecrets.rdsSecretName | quote }}
